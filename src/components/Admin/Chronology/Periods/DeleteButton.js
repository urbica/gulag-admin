import styled from 'styled-components';

export default styled.button`
  display: none;
  padding: 0;
  border: none;
  margin: 0;

  font-size: 16px;
  font-weight: 900;

  background-color: transparent;
`;
