import styled from 'styled-components';

export default styled.div`
  font-size: 36px;

  & span {
    display: inline-block;
    padding-top: 3px;

    vertical-align: top;

    font-size: 14px;

    opacity: 0.5;
  }
`;
