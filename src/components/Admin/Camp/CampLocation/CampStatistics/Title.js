import styled from 'styled-components';

export default styled.div`
  grid-column: span 7;

  padding: 6px 0;

  border-bottom: 1px solid rgba(0, 0, 0, .3);

  font-weight: bold;
  font-size: 12px;
`;
